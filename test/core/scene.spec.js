'use strict';

import {
    expect
} from 'chai/chai';
import {
    Scene
} from '../../src/core/scene';

describe('Scene tests', () => {
    describe('Constructor behavior', () => {
        beforeEach(function() {
        });

        it('Runs tests', () => {
            expect(true).to.be.equal(true);
        });
    });
});
